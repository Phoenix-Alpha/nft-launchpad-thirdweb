import { render } from '@testing-library/react'

import { NftCampaignForm } from '.'

describe('<NftCampaignForm />', () => {
  it('should renders correct <NftCampaignForm />', () => {
    render(<NftCampaignForm />)
  })
})
