import { render } from '@testing-library/react'

import { SiteHeader } from '.'

describe('<SiteHeader />', () => {
  it('should renders correct <SiteHeader />', () => {
    render(<SiteHeader />)
  })
})
