import { render } from '@testing-library/react'

import { SiteLayout } from '.'

describe('<SiteLayout />', () => {
  it('should renders correct <SiteLayout />', () => {
    render(<SiteLayout />)
  })
})
