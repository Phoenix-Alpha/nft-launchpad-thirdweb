import { render } from '@testing-library/react'

import { SignInWithWalletButton } from '.'

describe('<SignInWithWalletButton />', () => {
  it('should renders correct <SignInWithWalletButton />', () => {
    render(<SignInWithWalletButton />)
  })
})
