import { NextPage, GetServerSideProps } from 'next'

import { SiteHead, SiteLayout } from '@/components'
import { getServerSidePropsForProtectedPage } from '@/utils/pages'

export interface I{{pascalCase name}}PageProps {}

export const {{pascalCase name}}Page: NextPage<I{{pascalCase name}}PageProps> = ({}: I{{pascalCase name}}PageProps) => (
  <>
    <SiteHead />
    <SiteLayout />
  </>
)

export const getServerSideProps: GetServerSideProps = async (context) =>
    getServerSidePropsForProtectedPage(context)

{{pascalCase name}}Page.displayName = '{{pascalCase name}}Page'

export default {{pascalCase name}}Page
