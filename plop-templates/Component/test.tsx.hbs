import { render } from '@testing-library/react'

import { {{pascalCase name}} } from '.'

describe('<{{pascalCase name}} />', () => {
  it('should renders correct <{{pascalCase name}} />', () => {
    render(<{{pascalCase name}} />)
  })
})
